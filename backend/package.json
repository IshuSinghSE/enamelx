{
  "name": "backend",
  "version": "1.0.0",
  "main": "app.py",
  "scripts": {
    "start": "gunicorn app:app --bind 0.0.0.0:5000 --workers 4",
    "dev": "pip3 install -r requirements.txt &&  python3 app.py"
  },
  "dependencies": {
    "gunicorn": "^20.1.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Flask backend for the dental AI project"
}
